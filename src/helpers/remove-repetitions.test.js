const removeRepetitions = require('./remove-repetitions')

describe('Testing the helper removeRepetitions', () => {
  test('Should be a function', () => {
    expect(removeRepetitions).toBeInstanceOf(Object)
  })
})
